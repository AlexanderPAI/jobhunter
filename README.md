# Job Hunter

MVP AI-помощника для поиска и первичной фильтрации вакансий по резюме кандидата.

Версия: `0.2.0`

## Что делает проект

Job Hunter принимает резюме, извлекает из него профиль кандидата, формирует поисковый запрос, собирает вакансии с hh.ru и Хабр Карьеры, а затем фильтрует найденные вакансии под опыт и ожидания кандидата.

Профили, история подборов, найденные вакансии и результаты фильтрации сохраняются в PostgreSQL. Пользователь может просматривать сохранённые профили и повторять подбор, а при необходимости — экспортировать последнюю выдачу в CSV.

## Возможности

- Загрузка резюме через веб-интерфейс Streamlit.
- Поддержка файлов `.pdf`, `.docx`, `.doc`, `.txt`.
- Автоматическое извлечение профиля кандидата из резюме.
- Генерация поискового запроса для вакансий.
- Поиск вакансий на hh.ru.
- Поиск вакансий на Хабр Карьере.
- Объединение результатов hh.ru и Хабр Карьеры в единую выдачу.
- Удаление дублей по ссылкам.
- LLM-фильтрация вакансий по профилю кандидата.
- Сохранение профилей и истории подборов в PostgreSQL.
- Просмотр списка сохранённых профилей.
- Отдельная страница полного профиля с последним результатом поиска.
- Отображение даты и времени последнего подбора.
- Повторный подбор вакансий для сохранённого профиля.
- Просмотр результата в таблице внутри UI.
- Экспорт итоговой выдачи в CSV без промежуточного хранения CSV на backend.

## Как устроен проект

```text
jobhunter/
├── backend/                 # FastAPI backend и AI-агенты
│   ├── agents/
│   │   ├── cv_analyzer/     # анализ резюме и построение профиля
│   │   ├── searcher/        # поиск вакансий
│   │   └── vacancy_filter/  # фильтрация вакансий
│   ├── api/                 # HTTP API
│   ├── db/                  # SQLAlchemy 2.0 модели и репозитории
│   ├── alembic/             # миграции PostgreSQL
│   ├── llm_providers/       # адаптер OpenRouter
│   ├── utils/               # парсеры, чтение файлов, промпты
│   └── app.py               # запуск backend
├── frontend/                # многостраничный Streamlit UI
│   ├── db/                  # read-only подключение frontend к PostgreSQL
│   └── pages/               # список профилей и страница профиля
├── docker-compose.yaml      # запуск backend, frontend и PostgreSQL
├── pyproject.toml           # зависимости проекта
└── README.md
```

## Основной пользовательский сценарий

1. Пользователь открывает веб-интерфейс.
2. Загружает резюме.
3. Нажимает `Анализировать резюме`.
4. Система показывает профиль, краткое описание и поисковый запрос.
5. Пользователь нажимает `Подобрать вакансии`.
6. Backend собирает вакансии с hh.ru и Хабр Карьеры.
7. Агент фильтрует вакансии по профилю кандидата.
8. Пользователь смотрит таблицу и скачивает CSV.
9. Профиль и результаты остаются доступны на странице `Профили`.
10. С детальной страницы профиля можно повторить подбор вакансий.

## Технологии

- Python `3.13`
- FastAPI
- Streamlit
- LangGraph
- OpenRouter
- Playwright
- pandas
- aiohttp
- SQLAlchemy `2.0` (`Mapped`, `mapped_column`)
- Alembic
- PostgreSQL `17`
- asyncpg
- Docker Compose

## Переменные окружения

Для работы приложения нужны ключ OpenRouter и параметры подключения к PostgreSQL.

Создайте файл `.env` в корне проекта:

```env
OPENROUTER_KEY=your_openrouter_api_key
POSTGRES_USER=jobhunter
POSTGRES_PASSWORD=change_me
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=jobhunter
JWT_SECRET=replace_with_a_long_random_secret_at_least_32_chars
JWT_EXPIRE_MINUTES=480
```

Значения `OPENROUTER_KEY` и `JWT_SECRET` указывайте без кавычек: некоторые способы передачи `.env` в Docker сохраняют кавычки как часть секрета.

## Запуск через Docker Compose

```bash
docker compose up --build
```

После запуска:

- Frontend: `http://localhost:8501`

Backend и PostgreSQL не публикуют порты на хост. Backend доступен только frontend-контейнеру во внутренней Docker-сети.

## Пользователи и вход

Публичной регистрации нет. После запуска создайте пользователя из backend-контейнера:

```bash
docker compose exec backend python -m backend.create_user username
```

Скрипт интерактивно запросит пароль (минимум 8 символов). После этого пользователь может войти на `http://localhost:8501`. Все страницы интерфейса, API профилей, поисков и фильтрации требуют JWT, а история фильтруется по владельцу.

## Миграции

При запуске через Docker Compose backend автоматически выполняет `alembic upgrade head` после готовности PostgreSQL.

Для ручной работы с миграциями:

```bash
alembic -c backend/alembic.ini revision --autogenerate -m "migration_name"
alembic -c backend/alembic.ini upgrade head
```

## API

### Анализ резюме

```http
POST /v1/cv_analyzer/send_cv
```

Принимает файл резюме и возвращает:

- `search_prompt` - поисковый запрос для агента подбора вакансий;
- `user_profile` - структурированный профиль кандидата.
- `profile_id` - UUID сохранённого профиля.

### Поиск вакансий

```http
POST /v1/searcher/chat
```

Принимает:

- `message` - текст поискового запроса;
- `profile_id` - UUID профиля кандидата.

Возвращает `search_id` — UUID сохранённого запуска поиска. Вакансии сохраняются в PostgreSQL.

### Фильтрация вакансий

```http
POST /v1/filter/check
```

Принимает `search_id`, загружает профиль и найденные вакансии из PostgreSQL, сохраняет результат LLM-фильтрации и возвращает JSON со списком подходящих вакансий.

## Хранение данных

В PostgreSQL используются четыре основные сущности:

- `candidate_profiles` — структурированные профили кандидатов, исходный JSON и текст резюме;
- `search_runs` — история запусков поиска, запросы, фильтры и статистика;
- `vacancies` — дедуплицированные вакансии из разных источников;
- `search_results` — связь поиска с вакансией, порядок выдачи и результат фильтрации.

Frontend получает профили и историю только через защищённый backend API. Прямого сетевого доступа frontend к PostgreSQL нет.

## Ограничения

- Подбор зависит от качества распознавания резюме и ответа LLM.
- На данный момент поддержан только OpenRouter как провайдер LLM.
- Фильтрация вакансий выполняется батчами и может занимать время.
- Парсинг внешних сайтов зависит от доступности hh.ru и Хабр Карьеры.
- Профиль пока нельзя редактировать вручную из UI.
- Загруженные файлы резюме сохраняются локально в `backend/storage/cv`.

## Release Notes v.0.2.0

`v.0.2.0` переводит Job Hunter с файлового хранения результатов на PostgreSQL и добавляет историю профилей и подборов.

### Что вошло в релиз

- Добавлен PostgreSQL 17 в Docker Compose.
- Реализованы SQLAlchemy 2.0 модели только через `Mapped` и `mapped_column`.
- Добавлены таблицы профилей, запусков поиска, вакансий и результатов выдачи.
- Настроены связи между профилем, историей поисков и найденными вакансиями.
- Добавлена дедупликация вакансий в базе по внешней ссылке.
- Профиль сохраняется вместе с нормализованными полями, исходным JSON и текстом резюме.
- Поисковые запросы, фильтры, статистика и результат LLM-фильтрации сохраняются в PostgreSQL.
- CSV исключён из серверного процесса поиска и фильтрации.
- CSV оставлен как пользовательский экспорт, формируемый в памяти на frontend.
- Обновлены API-контракты: вместо путей к файлам используются `profile_id` и `search_id`.
- Добавлены Alembic-миграции и их автоматическое применение при старте backend.
- Backend и frontend ожидают готовности PostgreSQL перед запуском.
- Добавлен отдельный async-коннектор frontend к PostgreSQL.
- Интерфейс Streamlit разделён на страницы `Новый подбор`, `Профили` и детальную страницу профиля.
- На странице профилей отображаются имя, позиции последнего поиска и сокращённое описание.
- На детальной странице отображаются полный профиль, поисковый запрос, дата последнего подбора и последняя выдача.
- Добавлена кнопка `Подобрать вакансии снова`.
- Обработаны таймауты, пустые ответы OpenRouter и нестабильные типы данных от LLM.
- Исправлено формирование имён загруженных файлов.

### Цель релиза

Главная цель `v.0.2.0` — перейти от одноразового файлового сценария к постоянному хранению данных и повторному использованию профилей кандидатов.

Релиз должен обеспечить:

- сохранение профиля после анализа резюме;
- хранение истории поисков и результатов фильтрации;
- повторный подбор вакансий без повторной загрузки резюме;
- просмотр сохранённых профилей и последних результатов через UI;
- отказ от CSV как внутреннего формата обмена между агентами.

### Изменение потока данных

```text
резюме -> профиль в PostgreSQL -> запуск поиска -> вакансии в PostgreSQL
       -> LLM-фильтрация -> сохранённая выдача -> просмотр / экспорт CSV
```

### Что пока не входит в релиз

- Авторизация и регистрация пользователей.
- Разграничение доступа к профилям и истории поисков.
- Редактирование профиля кандидата перед повторным поиском.
- Удаление и архивирование профилей через UI.
- Просмотр полной истории всех подборов одного профиля.
- Настройка источников вакансий из интерфейса.
- Расширенная аналитика по вакансиям и динамике подборов.
- Автоматическая отправка откликов работодателям.
- Production-ready мониторинг, резервное копирование и управление секретами.

### Статус релиза

`v.0.2.0` сохраняет полный MVP-сценарий первой версии и добавляет постоянное хранилище, историю подборов и повторное использование профилей.

## Release Notes v.0.1.0

`v.0.1.0` - самая первая версия Job Hunter и MVP проекта.

### Что вошло в релиз

- Создан базовый backend на FastAPI.
- Создан frontend на Streamlit.
- Добавлен Docker Compose для запуска backend и frontend.
- Реализована загрузка резюме через UI.
- Добавлена поддержка резюме в форматах `.pdf`, `.docx`, `.doc`, `.txt`.
- Реализован агент анализа резюме `cv_analyzer`.
- Агент анализа резюме извлекает текст из файла и строит структурированный профиль кандидата.
- Реализована генерация поискового запроса на основе профиля кандидата.
- Реализован агент поиска вакансий `searcher`.
- Добавлен парсинг вакансий с hh.ru.
- Добавлен парсинг вакансий с Хабр Карьеры.
- Реализовано объединение результатов из разных источников в один CSV.
- Добавлена дедупликация вакансий по ссылке.
- Реализован агент фильтрации вакансий `vacancy_filter`.
- Фильтрация использует профиль кандидата и параметры вакансий.
- Добавлена батчевая обработка вакансий при фильтрации.
- Реализована выдача итогового CSV через API.
- В UI добавлен просмотр результатов в таблице.
- В UI добавлена кнопка скачивания CSV.
- Обновлен интерфейс: светлая палитра, вертикальные карточки профиля и единый блок статуса подбора.

### Цель релиза

Главная цель `v.0.1.0` - проверить полный путь от резюме до списка релевантных вакансий:

```text
резюме -> профиль кандидата -> поисковый запрос -> сбор вакансий -> фильтрация -> CSV
```

### Что пока не входит в релиз

- Авторизация пользователей.
- Сохранение истории поисков в базе данных.
- Редактирование профиля кандидата перед поиском.
- Настройка источников вакансий из UI.
- Расширенная аналитика по найденным вакансиям.
- Отправка откликов на вакансии.
- Production-ready деплой и мониторинг.

### Статус релиза

`v.0.1.0` считается MVP-релизом. Он пригоден для локального запуска, демонстрации основной идеи и дальнейшего развития продукта.
